# This file is generated. Do not edit.
# The configs are results of online config changes from UI/API/CLI.
# To persist configs in this file, copy the content to etc/base.hocon.
authentication = [
  {
    backend = built_in_database
    mechanism = password_based
    password_hash_algorithm {name = sha256, salt_position = suffix}
    user_id_type = username
  }
]
connectors {
  rabbitmq {
    AIR_Server_RabbitMQ {
      heartbeat = "30s"
      password = dfirpassword
      pool_size = 8
      port = 5672
      resource_opts {
        health_check_interval = "15s"
        start_timeout = "5s"
      }
      server = rabbitmq
      ssl {enable = false, verify = verify_peer}
      timeout = "5s"
      username = dfiruser
      virtual_host = "/"
    }
  }
}
log {
  console {}
  file {}
}
rule_engine {
  ignore_sys_message = true
  jq_function_default_timeout = "10s"
  rules {
    rule_0jdn {
      actions = []
      description = ""
      enable = true
      metadata {created_at = 1751994827248, last_modified_at = 1751994827248}
      sql = """~
        SELECT
          payload,
          topic as mqtt_topic,
          timestamp as msg_time
        FROM
          "health/+/+"~"""
    }
  }
}
